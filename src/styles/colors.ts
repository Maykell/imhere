import { Colors } from 'react-native/Libraries/NewAppScreen';

type Colors = {
  primary: string;
  secondary: string;
  gray: string;
  white: string;
  green: string;
  red: string;
};

const colors: Colors = {
  primary: '#202020',
  secondary: '#333533',
  gray: '#6b6b6b',
  white: '#ffffff',
  green: '#31cf67',
  red: '#e23c44',
};

export default colors;
